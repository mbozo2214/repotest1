<h1>Confirmar eliminación de la publicación <%= @publicacion.nombre %></h1>

<p>¿Estás seguro que deseas eliminar esta publicación?</p>

<%= form_with(model: @publicacion, url: eliminar_confirmacion_publicacion_path_path(@publicacion), local: true) do |form| %>
  <%= form.hidden_field :_method, value: 'delete' %>
  <%= form.submit 'Eliminar' %>
<% end %>

<%= link_to 'Cancelar', publicacions_path %>